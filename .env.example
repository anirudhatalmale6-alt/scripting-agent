# =============================================================================
# LOCAL DOCKER TESTING — fill in YOUR real values where marked [FILL IN]
# =============================================================================

# =============================================================================
# [ACTIVE] OpenAI — AI analysis + script generation + change detection
# =============================================================================
OPENAI_API_KEY=sk-proj-NY_58bsUqIsoHASFbNU5XVWo6dwAPYhCdfRG77oTyeWav_-FpR73oBCGWfRISztKyb_QrUPHjstluJPlkT5cA
OPENAI_MODEL=gpt-4o-mini
ENABLE_AI=true

# Use Docker sandbox for running test scripts (true/false)
# When true: k6 runs in grafana/k6 container, Selenium in maven container
# When false: runs directly on host (requires k6 + Java + Maven installed)
USE_SANDBOX=true

# =============================================================================
# [ACTIVE] Target app — mock-app container (always 200, simulates real app)
# =============================================================================
SFCC_SITE_URL=http://api:8000
ENV=dev

# =============================================================================
# [ACTIVE] GitHub — needed for PR diff detection + script generation
# [FILL IN] your personal access token and repo
# =============================================================================
GITHUB_TOKEN=github_pat_11AIS26ZIMy3L6D7QFtFF3zmZUS4vicN7mXF52WCCOlTM2trZF
GITHUB_REPO=cold-starr/testrepo
GITHUB_REPO_URL=https://api.github.com/repos/cold-starr/testrepo

# =============================================================================
# [ACTIVE] Grafana — handled by docker-compose (local Prometheus container)
# Leave all empty here — docker-compose injects the correct local URLs
# Only fill these in if running OUTSIDE Docker against Grafana Cloud
# =============================================================================
GRAFANA_URL=
GRAFANA_RW_URL=
GRAFANA_USERNAME=
GRAFANA_API_KEY=
K6_PROMETHEUS_RW_SERVER_URL=
K6_PROMETHEUS_RW_USERNAME=
K6_PROMETHEUS_RW_PASSWORD=

# =============================================================================
# [ACTIVE] Slack — final report posted here after every run
# [FILL IN] your real Slack incoming webhook URL
# =============================================================================
SLACK_WEBHOOK=https://hooks.slack.com/services/xxxx/xxxx/xxxxx

# =============================================================================
# [DISABLED] MCP — not needed locally, controller uses direct tool calls
# =============================================================================
MCP_URL=
TOKEN=

# =============================================================================
# [DISABLED] SpeedCurve, Datadog, Jira, Email — skipped gracefully
# =============================================================================
SPEEDCURVE_API_KEY=
SPEEDCURVE_SITE_ID=
DATADOG_API_KEY=
JIRA_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=
JIRA_PROJECT_KEY=
SMTP_SERVER=
SMTP_USER=
SMTP_PASS=
EMAIL_TO=

# =============================================================================
# [DISABLED] LangChain tracing — not needed locally
# =============================================================================
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=ai-performance-agent
OPENSPEC_URL=
K6_SCRIPT=script.js